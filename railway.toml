[build]
  builder = "dockerfile"
  dockerfilePath = "Dockerfile"

[deploy]
  num_replicas = 1
  restart_policy_type = "ON_FAILURE"
  restart_policy_max_retries = 3

[deploy.healthcheck]
  path = "/"
  port = 5000
